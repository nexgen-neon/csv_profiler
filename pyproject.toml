[project]
name = "example"
version = "0.1.0"
description = "Scalable data profiler"
readme = "README.md"
requires-python = ">=3.14"

dependencies = [
    "numpy",
    "pandas",
    "streamlit",
]

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"